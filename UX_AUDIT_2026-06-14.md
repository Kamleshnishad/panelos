# UI/UX Enhancement Tracker — 2026-06-14

Source: 3-agent UI/UX audit (shell/theme, forms, data-display). Executed infrastructure-first. All builds green.

Status: [x] done · [~] partial (pattern established, rollout pending) · [ ] open · [-] deferred

## Phase 1 — Foundation (design system)
- [x] P1a Global primitives in styles.css (.btn, .badge, .input, .data-table, type scale, .skeleton, toast styles, .sr-only)
- [x] P1b Cobalt migration — 364 indigo→token replacements across 38 files (script, UTF-8 safe; color-picker defaults preserved)
- [x] P1c Contrast — darkened `--text-3` to #6B7280 (WCAG AA); added `--text-faint` for decorative only

## Phase 2 — Feedback
- [x] P2 Global toast + promise-based confirm (`services/ui.js` + `UiHost.vue`, mounted in App.vue); replaced native alert()/confirm() in BoqRegister, QuotationList, StripePaymentForm

## Phase 3 — Data display
- [x] P3a SortIcon.vue created; QuotationList broken sort fixed + aria-sort + sortable affordance
- [x] P3b QuotationList: zebra striping, sticky header, tabular-nums, distinct hover (exemplar)
- [x] P3c SkeletonRows.vue created; wired into QuotationList (no more layout-jump)
- [~] P3a-c rollout to Order / Invoice / Batch / Dispatch / QC lists — PENDING (pattern + components ready to drop in)
- [ ] P3d Detail action-bar hierarchy (primary solid, group rest, isolate destructive)

## Phase 4 — Forms (QuotationCreate)
- [x] P4a Sticky live-totals bar (Subtotal/GST/Grand/Balance + save actions)
- [x] P4b Unsaved-changes guard (beforeunload + dirty Back confirm via confirm dialog)
- [x] P4d Fixed the dead "+ New Customer" — real add-customer modal (POST /customers), autofocus, selects on save
- [x] P4f Fixed-1m width now static label (was a readonly input); save success/error via toast
- [ ] P4c Inline/focus-first validation; disable modal Save until required filled (still submit-banner)
- [ ] P4e Collapse configured panel rows to summary; "Bottom same as top"
- [ ] P4f-2 Persistent DL legend; autofocus newly added size rows

## Phase 5 — Accessibility
- [~] P5a aria-labels — done on AppShell (logout, bell, search, hamburger) + UiHost + customer modal; rollout to list/detail icon buttons PENDING
- [ ] P5b Keyboard-operable list rows (tabindex/role/Enter)

## Phase 6 — Responsive
- [x] P6a Responsive sidebar (off-canvas + hamburger + scrim under 860px; search hidden, padding tightened)
- [ ] P6b Responsive form grids + scrollable size table
- [-] P6c Routing/deep-linking — deferred (large; would replace Manager pattern)

## Phase 7 — Functional bug fixes
- [x] P7a Reorder-level now persists — added POST /stock/coils|chemicals/{id}/reorder + service + wired CoilStock (was fake/local-only)
- [x] P7b Topbar: search now quick-jumps to sections; bell navigates to receivables (were dead) ; +aria-labels
- [x] (P3a) QuotationList `<sort-icon>` missing-component bug fixed

## Remaining (next batch)
- Roll table treatment (sort/zebra/sticky/skeleton) to the other 5 list tables
- Detail action-bar hierarchy (P3d) — esp. QuotationDetail's ~12 buttons
- Accessibility rollout (P5a icon labels everywhere, P5b keyboard rows)
- Responsive form grids (P6b)
- QuotationCreate: inline validation, row collapse, DL legend, autofocus (P4c/e/f-2)
