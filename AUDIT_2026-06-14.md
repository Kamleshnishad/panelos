# System Audit & Fix Tracker — 2026-06-14

Source: full multi-agent audit (backend models/migrations, backend services/controllers/PDF, frontend). Findings verified against live code + DB before recording. Fixed in priority sequence.

Status key: [ ] open · [x] done · [-] deferred · [FP] false positive (no action)

---

## Critical

- [x] **C1 — IDOR: GST breakdown not company-scoped.** `GstService::getGstBreakdown` now requires the invoice to belong to the caller's company (`whereHas('invoice', company_id)`); controller passes `auth()->user()->company_id`.
  - `backend/app/Http/Controllers/Api/GstController.php`, `backend/app/Services/GstService.php`
- [x] **C2 — Quotation update could re-point to another tenant's customer.** `QuotationService::update` now scopes `Customer` to the quotation's company; `customer_id` validation uses `Rule::exists(...)->where('company_id', …)`.
  - `backend/app/Services/QuotationService.php`, `backend/app/Http/Controllers/Api/QuotationController.php`

## High

- [x] **H3 — Accessory↔Quotation endpoints 500.** Added public `QuotationService::recalculateTotals()`; `AccessoryService` add/remove now call it. Verified add/remove recalc (6372 → 8142 → 6372).
  - `backend/app/Services/AccessoryService.php`, `backend/app/Services/QuotationService.php`
- [x] **H4 — Detail screens showed stale data after in-place nav.** Added `:key="detailId"` to all 5 managers (Quotation/Order/Invoice/Dispatch/Batch) so a new id forces reload.
- [x] **H5 — No global 401 / token-expiry handling.** Axios response interceptor in `main.js` clears creds + dispatches `auth:expired`; `App.vue` listens and returns to login.
- [x] **H6 — Dead `chemical()`/`coil` relations broke the whole Stock Dashboard.** Removed `ChemicalStock::chemical()`; repointed live call sites off the missing `Chemical`/`Coil` masters to `$item->name` / `panelType`. Also fixed: `getTotalStockValue` joined non-existent `coils`/`chemicals` tables (→ 0), added missing `ProductionBatch::dispatches()`, fixed `getExpiringChemicals($companyId)` passed as `$days` (filtered 1 day not 30). Stock Dashboard verified end-to-end. Deleted the two dead non-`Api` controllers (closes L3).
  - `backend/app/Models/ChemicalStock.php`, `StockDashboardService.php`, `StockService.php`, `ProductionBatch.php`

## Medium

- [x] **M1 — No duplicate-invoice guard.** Added existing-non-cancelled-invoice guards in `createFromOrder` and `createFromDispatch`.
- [x] **M2 — Invoice PDF.** ₹ instead of `$`; item desc uses `panelType->name`/`thickness`; Bill-To resolves customer from dispatch OR order with GSTIN; `generatePdf` eager-loads `company` + `order.customer`.
- [x] **M3 — Global unique rules → per-company.** Accessory `code` (store+update) and HSN `code` now scoped per company via `Rule::unique()->where('company_id', …)`. GSTIN left global (unique per legal entity by nature).
- [x] **M4 — `CustomerController::update` had no validation.** Added a full validation ruleset.
- [x] **M5 — Accessory attach not tenant-scoped.** `AccessoryService::addToQuotation` now scopes `Accessory` to the quotation's company.
- [x] **M6 — `QuotationItemSize` `sqm` accessor/cast shadowed the generated DB column.** Removed accessor + cast; reads the stored column. Verified (12.0000 from DB).
- [x] **M7 — `rates_pending` accessor comment reconciled.** Accessor kept (flags draft/converted-BOQ awaiting rates); misleading comment corrected.
- [x] **M8 — Fixed 1m width baked into SQM math.** Formula reads `width_mm` instead of the `(1000/1000)` literal (behaviour identical today).

## Low

- [x] **L1 — Quotation/Order number sequence race.** Added unique indexes `(company_id, quotation_no)` and `(company_id, order_no)` (migration). Full retry-on-collision noted as future hardening.
- [x] **L2 — `days_overdue` unsigned Carbon diff.** Now computed only when past due, `due_date->diffInDays(now())` floored.
- [x] **L3 — Dead duplicate controllers.** Deleted (during H6).
- [x] **L4 — Orphan table `quotation_panel_items`.** Dropped (empty, no model) in the L1 migration.
- [x] **L5 — Migration `2026_06_06_000001` rollback hazard.** `down()` made a no-op (matches the no-op `up()`).
- [x] **L6 — QuotationCreate hardcoded `#1a237e`.** Recolored to `var(--primary)` (cobalt). (Dead topbar search/bell left as a minor cosmetic note.)
- [-] **L7 — `view-*` events drop their id payload** (navigate to list instead of deep-linking). Deferred — UX enhancement touching navigation wiring, not a bug.

## False positives (verified, no action)

- Panel/Accessory "missing columns" — columns exist in live DB (`category,hsn_code,available_thicknesses,unit,rate`).
- Gutter/padding bug — not present.
- SoftDeletes trap — no offending model.

---

### Remaining open items
- **L7** (deferred): deep-link the clicked record when navigating between modules.
- **L1 follow-up** (optional): retry-on-unique-collision loop in number generation (indexes now prevent corrupt data regardless).
